apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: kafka
  annotations:
    fluxcd.io/automated: "true"
spec:
  releaseName: kafka
  chart:
    repository: https://charts.bitnami.com/bitnami
    name: kafka
    version: 12.6.2
  values:
    image:
      tag: 2.7.0
    replicaCount: 1
