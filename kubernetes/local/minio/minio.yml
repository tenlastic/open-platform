apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: minio
  annotations:
    fluxcd.io/automated: "true"
spec:
  releaseName: minio
  chart:
    repository: https://charts.bitnami.com/bitnami
    name: minio
    version: 6.0.2
  values:
    accessKey:
      password: username
    image:
      tag: 2021.1.16
    secretKey:
      password: password
