apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: article-api
  labels:
    app: article-api
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: article-api
    spec:
      containers:
        - name: article-api
          image: node:10
          command: ["npm", "run", "start"]
          envFrom:
            - secretRef:
                name: nodejs-environment-variables
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 50m
          volumeMounts:
            - name: app
              mountPath: /usr/src/app/
          workingDir: /usr/src/app/projects/javascript/nodejs/applications/article-api/
      volumes:
        - name: app
          hostPath:
            path: /c/open-platform/
---
apiVersion: v1
kind: Service
metadata:
  name: article-api
  labels:
    app: article-api
    service: article-api
spec:
  ports:
    - port: 3000
      name: http
  selector:
    app: article-api
