apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: sealed-secrets
  namespace: static
spec:
  chart:
    spec:
      chart: kubernetes/helm/sealed-secrets/
      sourceRef:
        kind: GitRepository
        name: open-platform
  interval: 5m
  releaseName: sealed-secrets
