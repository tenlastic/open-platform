apiVersion: apps/v1
kind: StatefulSet
metadata:
  labels:
    app: cdc
  name: cdc
  namespace: static
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cdc
  serviceName: cdc
  template:
    metadata:
      labels:
        app: cdc
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: tenlastic.com/low-priority
                    operator: Exists
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 1
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - cdc
                topologyKey: kubernetes.io/hostname
      containers:
        - name: cdc
          image: tenlastic/cdc:0.0.314
          env:
            - name: MONGO_DATABASE_NAME
              value: api
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          envFrom:
            - secretRef:
                name: nodejs
          resources:
            requests:
              cpu: 25m
              memory: 75Mi
