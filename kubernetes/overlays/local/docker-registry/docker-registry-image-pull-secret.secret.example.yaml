apiVersion: v1
kind: Secret
metadata:
  name: docker-registry-image-pull-secret
stringData:
  .dockerconfigjson: |-
    {
      "auths":
        {
          "docker-registry.default:5000":
            {
              "username": "username",
              "password": "password"
            }
        }
    }
type: kubernetes.io/dockerconfigjson
