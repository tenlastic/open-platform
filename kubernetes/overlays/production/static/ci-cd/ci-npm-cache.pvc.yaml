apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ci-npm-cache
  namespace: static
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi
  storageClassName: balanced-expandable
