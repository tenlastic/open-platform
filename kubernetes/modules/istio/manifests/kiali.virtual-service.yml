apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: kiali
  namespace: istio-system
spec:
  hosts:
  - kiali.tenlastic.com
  gateways:
  - istio-autogenerated-k8s-ingress
  http:
  - route:
    - destination:
        host: kiali
        port:
          number: 20001
