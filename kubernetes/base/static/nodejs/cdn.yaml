apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: cdn
  name: cdn
  namespace: static
spec:
  replicas: 3
  selector:
    matchLabels:
      app: cdn
  template:
    metadata:
      labels:
        app: cdn
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: tenlastic.com/low-priority
                    operator: Exists
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 1
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - cdn
                topologyKey: kubernetes.io/hostname
      containers:
        - name: api
          image: tenlastic/api:0.0.206
          envFrom:
            - secretRef:
                name: nodejs-environment-variables
          livenessProbe:
            httpGet:
              path: /articles
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 50m
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: cdn
    service: cdn
  name: cdn
  namespace: static
spec:
  ports:
    - port: 3000
      name: http
  selector:
    app: cdn
