apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  annotations:
    fluxcd.io/automated: "true"
  name: sealed-secrets
  namespace: static
spec:
  chart:
    git: https://github.com/tenlastic/open-platform
    path: kubernetes/helm/sealed-secrets/
    ref: master
    skipDepUpdate: true
  releaseName: sealed-secrets
