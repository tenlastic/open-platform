apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: database-api
  labels:
    app: database-api
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: database-api
    spec:
      containers:
        - name: database-api
          image: docker.pkg.github.com/tenlastic/open-platform/database-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: cloud.google.com/gke-preemptible
                    operator: Exists
