apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: database-api
  labels:
    app: database-api
spec:
  replicas: 3
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: true
      labels:
        app: database-api
    spec:
      containers:
        - name: database-api
          image: tenlastic/database-api:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 50m
          envFrom:
            - secretRef:
                name: database-api-environment-variables
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: cloud.google.com/gke-preemptible
                    operator: Exists
---
apiVersion: v1
kind: Service
metadata:
  name: database-api
  labels:
    app: database-api
    service: database-api
spec:
  ports:
    - port: 3000
      name: http
  selector:
    app: database-api
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: database-api
spec:
  hosts:
    - database-api.tenlastic.com
  gateways:
    - istio-system/istio-autogenerated-k8s-ingress
  http:
    - route:
        - destination:
            host: database-api
            port:
              number: 3000
