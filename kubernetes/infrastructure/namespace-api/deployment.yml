apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: namespace-api
  labels:
    app: namespace-api
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: namespace-api
    spec:
      containers:
        - name: namespace-api
          image: tenlastic/namespace-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: 50m
          envFrom:
            - secretRef:
                name: namespace-api-environment-variables
      imagePullSecrets:
        - name: githubimagepullsecret
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: cloud.google.com/gke-preemptible
                    operator: Exists
