apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: sso-ui
  labels:
    app: sso-ui
spec:
  replicas: 3
  template:
    metadata:
      annotations:
        sidecar.istio.io/inject: "true"
      labels:
        app: sso-ui
        version: 0.0.9
    spec:
      containers:
        - name: sso-ui
          image: tenlastic/sso-ui:0.0.9
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 50m
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: cloud.google.com/gke-preemptible
                    operator: Exists
---
apiVersion: v1
kind: Service
metadata:
  name: sso-ui
  labels:
    app: sso-ui
    service: sso-ui
spec:
  ports:
    - port: 80
      name: http
  selector:
    app: sso-ui
---
apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: sso-ui
spec:
  hosts:
    - www.tenlastic.com
  gateways:
    - istio-system/istio-autogenerated-k8s-ingress
  http:
    - route:
        - destination:
            host: sso-ui
            port:
              number: 80
