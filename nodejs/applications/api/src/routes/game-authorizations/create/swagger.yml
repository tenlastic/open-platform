paths:
  /namespaces/{namespaceId}/game-authorizations:
    post:
      summary: Create
      tags: [GameAuthorizations]
      description: Creates a new record.
      security:
        - AccessToken: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/GameAuthorization'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  record:
                    $ref: '#/components/schemas/GameAuthorization'
