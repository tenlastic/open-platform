components:
  schemas:
    WorkflowStatus:
      type: object
      properties:
        finishedAt:
          type: string
          format: date-time
        nodes:
          type: array
          items:
            type: object
            properties:
              _id:
                type: string
              children:
                type: array
                items:
                  type: string
              displayName:
                type: string
              finishedAt:
                type: string
                format: date-time
              message:
                type: string
              name:
                type: string
              outboundNodes:
                type: array
                items:
                  type: string
              phase:
                type: string
              startedAt:
                type: string
                format: date-time
              templateName:
                type: string
              type:
                type: string
        phase:
          type: string
          enum: [Error]
        startedAt:
          type: string
          format: date-time
