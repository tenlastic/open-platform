components:
  schemas:
    QueueThreshold:
      type: object
      properties:
        rating:
          type: number
        seconds:
          type: number
        usersPerTeam:
          type: array
          items:
            type: number
            minimum: 1
      required:
        - seconds
        - usersPerTeam
