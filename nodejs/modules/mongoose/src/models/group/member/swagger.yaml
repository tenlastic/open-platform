components:
  schemas:
    GroupMember:
      type: object
      properties:
        _id:
          type: string
          format: ^[0-9a-f]{24}$
        userId:
          type: string
          format: ^[0-9a-f]{24}$
        webSocketId:
          type: string
          format: ^[0-9a-f]{24}$
      required:
        - userId
        - webSocketId
