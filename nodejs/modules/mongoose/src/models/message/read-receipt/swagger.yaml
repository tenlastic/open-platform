components:
  schemas:
    MessageReadReceipt:
      type: object
      properties:
        createdAt:
          type: string
          format: date-time
        userId:
          type: string
          format: ^[0-9a-f]{24}$
      required:
        - userId
