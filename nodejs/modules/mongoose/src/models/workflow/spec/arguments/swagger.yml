components:
  schemas:
    WorkflowSpecArguments:
      type: object
      properties:
        parameters:
          type: array
          items:
            $ref: '#/components/schemas/WorkflowSpecParameter'
