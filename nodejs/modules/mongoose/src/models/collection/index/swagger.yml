components:
  schemas:
    CollectionIndex:
      type: object
      properties:
        key:
          type: object
          additionalProperties:
            enum: [-1, 1]
            type: number
        options:
          $ref: '#/components/schemas/CollectionIndexOptions'
      required:
        - key
