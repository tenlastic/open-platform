components:
  schemas:
    GameServerProbesProbeHttpHeader:
      type: object
      properties:
        name:
          type: string
        value:
          type: string
      required:
        - name
        - value
