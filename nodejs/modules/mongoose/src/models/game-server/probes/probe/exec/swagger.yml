components:
  schemas:
    GameServerProbesProbeExec:
      type: object
      properties:
        command:
          type: array
          items:
            type: string
      required:
        - command
