components:
  schemas:
    GameServerProbes:
      type: object
      properties:
        liveness:
          $ref: '#/components/schemas/GameServerProbesProbe'
        readiness:
          $ref: '#/components/schemas/GameServerProbesProbe'
