components:
  schemas:
    NamespaceLimits:
      type: object
      properties:
        bandwidth:
          type: number
        cpu:
          type: number
        memory:
          type: number
        preemptible:
          type: boolean
        storage:
          type: number
