components:
  schemas:
    NamespaceLimits:
      type: object
      properties:
        cpu:
          type: integer
        memory:
          type: integer
        preemptible:
          type: boolean
        storage:
          type: integer
