import { getCredentials, setCredentials } from './credentials/credentials';
import { send } from './send/send';

import { stub } from './stub';

export default { getCredentials, send, setCredentials, stub };
