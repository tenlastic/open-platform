components:
  schemas:
    {{namePascalSingular}}:
      type: object
      properties:
        _id:
          type: string
          pattern: '^[a-f0-9]{24}$'
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
