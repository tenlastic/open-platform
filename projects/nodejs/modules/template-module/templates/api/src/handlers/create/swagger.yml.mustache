paths:
  /{{nameCamelPlural}}:
    post:
      summary: Create
      tags: [{{namePascalSingular}} Service]
      description: Creates a new record.
      security:
        - AccessToken: []
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/{{namePascalSingular}}'
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  record:
                    $ref: '#/components/schemas/{{namePascalSingular}}'
