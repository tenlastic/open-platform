import { connect, getAdmin, getConnection, getProducer } from './connect';
import { createTopic } from './create-topic';

export default { connect, createTopic, getAdmin, getConnection, getProducer };
