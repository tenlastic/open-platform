components:
  schemas:
    Game:
      type: object
      properties:
        _id:
          type: string
          format: ^[a-f0-9]{24}$
        createdAt:
          type: string
          format: date-time
        description:
          type: string
        icon:
          type: string
        images:
          type: array
          items:
            type: string
        namespaceId:
          type: string
          format: ^[a-f0-9]{24}$
        subtitle:
          type: string
          format: ^.{2,40}$
        title:
          type: string
          format: ^.{2,40}$
        updatedAt:
          type: string
          format: date-time
        videos:
          type: array
          items:
            type: string
