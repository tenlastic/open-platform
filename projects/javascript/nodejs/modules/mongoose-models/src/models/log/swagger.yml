components:
  schemas:
    Log:
      type: object
      properties:
        _id:
          type: string
          format: ^[a-f0-9]{24}$
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        gameServerId:
          type: string
          format: ^[a-f0-9]{24}$
        unix:
          type: number
          format: double
        updatedAt:
          type: string
          format: date-time
