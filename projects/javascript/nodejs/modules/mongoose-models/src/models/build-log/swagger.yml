components:
  schemas:
    BuildLog:
      type: object
      properties:
        _id:
          type: string
          format: ^[0-9a-f]{24}$
        body:
          type: string
        buildId:
          type: string
          format: ^[0-9a-f]{24}$
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        namespaceId:
          type: string
          format: ^[0-9a-f]{24}$
        nodeId:
          type: string
        unix:
          type: number
          format: double
        updatedAt:
          type: string
          format: date-time
