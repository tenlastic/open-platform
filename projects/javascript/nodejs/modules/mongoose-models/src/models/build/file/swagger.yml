components:
  schemas:
    BuildFile:
      type: object
      properties:
        compressedBytes:
          type: integer
        md5:
          type: string
        path:
          type: string
        uncompressedBytes:
          type: integer
      required:
        - compressedBytes
        - md5
        - path
        - uncompressedBytes
