components:
  schemas:
    BuildReference:
      type: object
      properties:
        _id:
          type: string
          format: ^[0-9a-f]{24}$
        files:
          type: array
          items:
            type: string
      required:
        - _id
