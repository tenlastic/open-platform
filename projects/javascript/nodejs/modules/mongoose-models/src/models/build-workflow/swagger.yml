components:
  schemas:
    BuildWorkflow:
      type: object
      properties:
        _id:
          type: string
          format: ^[a-f0-9]{24}$
        buildId:
          type: string
          format: ^[a-f0-9]{24}$
        createdAt:
          type: string
          format: date-time
        namespaceId:
          type: string
          format: ^[a-f0-9]{24}$
        spec:
          $ref: '#/components/schemas/WorkflowSpec'
        status:
          $ref: '#/components/schemas/WorkflowStatus'
        updatedAt:
          type: string
          format: date-time
