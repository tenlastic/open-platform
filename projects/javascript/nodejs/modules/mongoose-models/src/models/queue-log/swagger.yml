components:
  schemas:
    QueueLog:
      type: object
      properties:
        _id:
          type: string
          format: ^[0-9a-f]{24}$
        body:
          type: string
        createdAt:
          type: string
          format: date-time
        expiresAt:
          type: string
          format: date-time
        namespaceId:
          type: string
          format: ^[0-9a-f]{24}$
        queueId:
          type: string
          format: ^[0-9a-f]{24}$
        unix:
          type: number
          format: double
        updatedAt:
          type: string
          format: date-time
