components:
  schemas:
    WorkflowStatus:
      type: object
      properties:
        finishedAt:
          type: string
          format: date-time
        phase:
          type: string
          enum: [Error]
        startedAt:
          type: string
          format: date-time
