paths:
  /databases/{databaseId}/web-sockets/count:
    get:
      summary: Count
      tags: [DatabaseWebSockets]
      description: Returns the number of matching records for the query.
      security:
        - AccessToken: []
      parameters:
        - in: path
          name: databaseId
          schema:
            type: string
          required: true
      responses:
        200:
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  count:
                    type: integer
