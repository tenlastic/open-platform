apiVersion: batch/v1
kind: Job
metadata:
  labels:
    app: social-api-migrations
  name: social-api-migrations
  namespace: static
spec:
  template:
    metadata:
      name: social-api-migrations
      labels:
        app: social-api-migrations
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: tenlastic.com/low-priority
                    operator: Exists
      containers:
        - name: social-api-migrations
          image: tenlastic/social-api-migrations:latest
          envFrom:
            - secretRef:
                name: nodejs
          resources:
            requests:
              cpu: 50m
      restartPolicy: OnFailure
  ttlSecondsAfterFinished: 0
