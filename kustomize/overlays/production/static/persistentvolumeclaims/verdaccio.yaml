kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: verdaccio
  namespace: static
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10G
  storageClassName: balanced-expandable
