version: "3.7"
services:
  development-cli:
    build:
      context: ./dockerfiles/development-cli/
    volumes:
      - ~/.config/gcloud/:/root/.config/gcloud/
      - ~/.kube/config/:/.kube/config/
      - ./:/home/
